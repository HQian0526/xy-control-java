<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.springboottemplate.mapper.system.UserMapper">
    <!-- 定义结果映射 -->
    <resultMap id="userResultMap" type="User">
        <id property="id" column="id"/> <!-- 主键字段 -->
        <result property="userName" column="user_name"/>
        <result property="realName" column="real_name"/>
        <result property="sex" column="sex"/>
        <result property="phone" column="phone"/>
        <result property="avatar" column="avatar"/>
        <result property="address" column="address"/>
        <result property="birthday" column="birthday"/>
        <result property="updateTime" column="update_time"/>
        <result property="createdTime" column="created_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="createdBy" column="created_by"/>
        <result property="email" column="email"/>
        <result property="identityType" column="identity_type"/>
        <result property="bindStoreId" column="bind_store_id"/>
        <result property="openid" column="openid"/>
        <result property="unionid" column="unionid"/>
        <result property="password" column="password"/>
        <result property="remark" column="remark"/>
        <result property="deleted" column="deleted"/>
    </resultMap>

    <!-- 新增用户 -->
    <insert id="addUser" parameterType="User" useGeneratedKeys="true" keyProperty="id">
        INSERT into user (user_name, password, real_name, sex, phone, avatar, address, birthday, email, identity_type, bind_store_id, openid, unionid, created_time, created_by, remark)
        value (#{userName}, #{password}, #{realName}, #{sex}, #{phone}, #{avatar}, #{address}, #{birthday}, #{email}, #{identityType}, #{bindStoreId}, #{openid}, #{unionid}, #{createdTime}, #{createdBy}, #{remark})
    </insert>

    <!-- 查询所有用户信息（模糊查询）-->
    <select id="findUser" parameterType="User" resultMap="userResultMap">
        select * from user
        <where>
            <if test="id != null and id != ''">
                and id LIKE CONCAT('%', #{id}, '%')
            </if>
            <if test="userName != null and userName != ''">
                and user_name like CONCAT('%', #{userName}, '%')
            </if>
            <if test="realName != null and realName != ''">
                and real_name like CONCAT('%', #{realName}, '%')
            </if>
            <if test="sex != null and sex != ''">
                and sex = #{sex}
            </if>
            <if test="phone != null and phone != ''">
                and phone like CONCAT('%', #{phone}, '%')
            </if>
            <if test="address != null and address != ''">
                and address like CONCAT('%', #{address}, '%')
            </if>
            <if test="birthday != null and birthday != ''">
                and birthday like CONCAT('%', #{birthday}, '%')
            </if>
            <if test="email != null and email != ''">
                and email like CONCAT('%', #{email}, '%')
            </if>
            <if test="identityType != null">
                and identity_type = #{identityType}
            </if>
            <if test="bindStoreId != null">
                and bind_store_id = #{bindStoreId}
            </if>
            <if test="createdTime != null and createdTime != ''">
                and created_time = #{createdTime}
            </if>
            <if test="updateTime != null and updateTime != ''">
                and update_time = #{updateTime}
            </if>
            <if test="createdBy != null and createdBy != ''">
                and created_by = #{createdBy}
            </if>
            <if test="updateBy != null and updateBy != ''">
                and update_by = #{updateBy}
            </if>
            <choose>
                <when test="deleted != null">
                    and deleted = #{deleted}
                </when>
<!--                <otherwise>-->
<!--                    and deleted = 0-->
<!--                </otherwise>-->
            </choose>
        </where>
    </select>

    <!-- 修改用户信息 -->
    <update id="updateUser" parameterType="User">
        update user
        <set>
            <if test="userName != null">user_name = #{userName},</if>
            <if test="password != null">password = #{password},</if>
            <if test="realName != null">real_name = #{realName},</if>
            <if test="sex != null">sex = #{sex},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="avatar != null">avatar = #{avatar},</if>
            <if test="address != null">address = #{address},</if>
            <if test="birthday != null">birthday = #{birthday},</if>
            <if test="email != null">email = #{email},</if>
            <if test="identityType != null">identity_type = #{identityType},</if>
            <if test="bindStoreId != null">bind_store_id = #{bindStoreId},</if>
            <if test="openid != null">openid = #{openid},</if>
            <if test="unionid != null">unionid = #{unionid},</if>
            <if test="remark != null">remark = #{remark},</if>
            <if test="deleted != null">deleted = #{deleted},</if>
        </set>
        where id = #{id}
    </update>

    <!-- 解除微信身份并逻辑删除（先清空 openid，避免唯一索引冲突） -->
    <update id="detachWxAndSoftDelete" parameterType="java.lang.Integer">
        update user
        set openid = null,
            unionid = null,
            deleted = 1
        where id = #{id}
    </update>

    <!-- 根据用户名精确查询用户信息-->
    <select id="selectByUserName" parameterType="java.lang.String" resultMap="userResultMap">
        select * from user where user_name = #{userName} and deleted = 0 limit 1
    </select>

    <!-- 根据微信 openid 精确查询用户信息-->
    <select id="selectByOpenid" parameterType="java.lang.String" resultMap="userResultMap">
        select * from user where openid = #{openid} and deleted = 0 limit 1
    </select>

    <!-- 根据手机号精确查询用户信息-->
    <select id="selectByPhone" parameterType="java.lang.String" resultMap="userResultMap">
        select * from user where phone = #{phone} and deleted = 0 limit 1
    </select>
</mapper>