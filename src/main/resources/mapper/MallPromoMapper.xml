<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.springboottemplate.mapper.MallPromoMapper">
    <resultMap id="mallPromoResultMap" type="MallPromo">
        <id property="id" column="id"/>
        <result property="storeId" column="store_id"/>
        <result property="name" column="name"/>
        <result property="status" column="status"/>
        <result property="forever" column="forever"/>
        <result property="endTime" column="end_time"/>
        <result property="createdBy" column="created_by"/>
        <result property="createdTime" column="created_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateTime" column="update_time"/>
        <result property="deleted" column="deleted"/>
    </resultMap>

    <insert id="addMallPromo" parameterType="MallPromo">
        INSERT INTO mall_promo (
            id, store_id, name, status, forever, end_time,
            created_by, created_time, deleted
        ) VALUES (
            #{id}, #{storeId}, #{name}, #{status}, #{forever}, #{endTime},
            #{createdBy}, #{createdTime}, #{deleted}
        )
    </insert>

    <select id="findMallPromo" parameterType="MallPromo" resultMap="mallPromoResultMap">
        SELECT * FROM mall_promo
        <where>
            <if test="id != null">AND id = #{id}</if>
            <if test="storeId != null">AND store_id = #{storeId}</if>
            <if test="name != null and name != ''">AND name LIKE CONCAT('%', #{name}, '%')</if>
            <if test="status != null">AND status = #{status}</if>
            <choose>
                <when test="deleted != null">AND deleted = #{deleted}</when>
                <otherwise>AND deleted = 0</otherwise>
            </choose>
        </where>
        ORDER BY created_time DESC, id DESC
    </select>

    <select id="selectPromoById" resultMap="mallPromoResultMap">
        SELECT * FROM mall_promo WHERE id = #{id} AND deleted = 0 LIMIT 1
    </select>

    <select id="selectActiveByStoreId" resultMap="mallPromoResultMap">
        SELECT * FROM mall_promo
        WHERE store_id = #{storeId}
          AND deleted = 0
          AND status = 1
          AND (
            forever = 1
            OR (forever = 0 AND end_time IS NOT NULL AND end_time &gt; #{now})
          )
        ORDER BY created_time DESC, id DESC
        LIMIT 1
    </select>

    <select id="countActiveByStoreId" resultType="int">
        SELECT COUNT(1) FROM mall_promo
        WHERE store_id = #{storeId}
          AND deleted = 0
          AND status = 1
          AND (
            forever = 1
            OR (forever = 0 AND end_time IS NOT NULL AND end_time &gt; #{now})
          )
          <if test="excludeId != null">
              AND id != #{excludeId}
          </if>
    </select>

    <update id="updateMallPromo" parameterType="MallPromo">
        UPDATE mall_promo
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="status != null">status = #{status},</if>
            <if test="forever != null">forever = #{forever},</if>
            <if test="endTime != null">end_time = #{endTime},</if>
            <if test="forever != null and forever == 1">end_time = NULL,</if>
            <if test="updateBy != null">update_by = #{updateBy},</if>
            <if test="deleted != null">deleted = #{deleted},</if>
        </set>
        WHERE id = #{id}
    </update>
</mapper>
